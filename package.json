{
  "name": "allstonf-github-io",
  "version": "2.0.0",
  "private": true,
  "description": "Astro rebuild of allstonf.github.io, rendering the same content/profile.json content model that build/build.py renders for v1.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run",
    "verify:content": "node scripts/verify-content.mjs",
    "verify:all": "npm run build && npm test && node scripts/verify-content.mjs --check-urls && node scripts/check-js-budget.mjs"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
